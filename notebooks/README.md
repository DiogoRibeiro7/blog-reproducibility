# Notebooks

Notebooks are kept only when an interactive derivation or exploration materially improves reproducibility.

A notebook should have a descriptive name, deterministic execution where practical, no `.ipynb_checkpoints`, and a corresponding tested implementation under `src/blog_reproducibility/` when the computation supports published claims.

Untitled scratch notebooks should not be migrated.
